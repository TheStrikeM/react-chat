import React from 'react'
import {Redirect, Switch, Route} from 'react-router-dom'

function AppRoute() {
    const user = false
    return (
        <div>
            
        </div>
    )
}

export default AppRoute
